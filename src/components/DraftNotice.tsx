export function DraftNotice() {
  return (
    <div className="bg-amber-50 border border-amber-300 text-amber-900 text-sm rounded-lg px-4 py-3 mb-6">
      <strong>Szkic dokumentu.</strong> Dane podmiotu są uzupełnione, ale treść nadal wymaga weryfikacji przez
      prawnika przed publikacją (m.in. potwierdzenia zapisów o przekazywaniu danych poza EOG i okresie ich
      przechowywania). Usuń ten baner, gdy dokument zostanie zatwierdzony.
    </div>
  );
}
